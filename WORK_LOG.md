# 📝 Ultimate AI Audio Suite - 開発記録 (Development Log)

## 📅 プロジェクト進捗状況
*   **ステータス**: ベータフェーズ（安定化および機能追加中）
*   **最終更新**: 2026-05-01

---

## 🚀 実施済みの主要な作業内容

### 1. C++ ネイティブ移植 (Core Migration)
*   **Python 脱却**: PyTorch 依存を排除し、ONNX Runtime (C++ API) を核としたエンジンを構築。
*   **高速推論**: DirectML エグゼキューション・プロバイダーの統合により、Windows 標準の GPU 加速を実装。
*   **DSP 実装**: STFT、iSTFT、メルフィルターバンク、Griffin-Lim 等の DSP アルゴリズムを C++ でフルスクラッチ実装。

### 2. Ultimate Dashboard の構築 (UI/UX)
*   **マルチタブ設計**: SINGLE（個別）、BATCH（一括）、LIVE（リアルタイム）、Settings（設定）の 4 タブ構成。
*   **可視化エンジン**: ImGui + ImPlot によるリアルタイム波形およびスペクトログラム表示。
*   **FFmpeg 統合**: ほぼすべての音声フォーマットに対応した読み込み・保存機能を実装。

### 3. モデル自動管理 (Automation)
*   **Model Downloader**: 必要な AI モデルファイルを自動的にクラウドから取得し、セットアップする機能を実装。

---

## 🛠️ 最新のデバッグと修正内容 (2026-05-01)

### 🐛 修正済み: アクセス違反 (0xC0000005) の解消
*   **現象**: 音声処理開始時にアプリケーションが強制終了する。
*   **原因**: `AIEnhancer.cpp` 内で Denoiser モデルへの入力を構成する際、ビン数（1025固定）と実際の STFT 結果の不整合により、配列外のメモリにアクセスしていた。
*   **対策**: `n_bins` を動的に取得し、モデルの期待値と一致する場合のみ処理を続行する安全策を導入。

### 📁 修正済み: ファイル選択ダイアログの挙動改善
*   **現象**: 「Browse」ボタンをクリックしてもダイアログが表示されない場合がある。
*   **原因**: COM (Component Object Model) の初期化が STA (Single Threaded Apartment) モードで正しく維持されていなかった。
*   **対策**: `openFileDialog` 関数内で `CoInitializeEx` の戻り値を厳密にチェックし、初期化済みの場合は既存のコンテキストを利用するように修正。また、詳細なデバッグログを追加。

---

## 📅 今後の計画 (Next Steps)
1.  **リアルタイム推論の安定化**: LIVE モニターでの AI 処理適用（現在はパススルー中心）。
2.  **配布パッケージの最適化**: 依存 DLL の最小化と、単一の `.exe` または インストーラー形式へのパッケージング。
3.  **多言語対応の完全化**: UI の日本語/英語切り替えのさらなるブラッシュアップ。

---
Produced by **Antigravity AI**
